# SSH host (or alias) of the machine running the backend
HOST=my-server
# Where to place the code on that machine
REMOTE_DIR=~/projects/bypp-backend
# Local port uvicorn listens on
PORT=8787
# Tailscale Funnel HTTPS port (443, 8443 or 10000)
FUNNEL_PORT=8443
