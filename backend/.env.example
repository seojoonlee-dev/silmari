APP_PASSWORD=change-me
APP_SECRET=long-random-string
PORT=8787
LLM_BASE_URL=http://localhost:8000/v1
LLM_MODEL=qwen3.6-35b-a3b
# Run the vision model on every Nth captured frame; the rest inherit the last analysis.
ANALYZE_EVERY=5
# Seconds a window can go unseen before it counts as closed.
CLOSE_AFTER_S=600
